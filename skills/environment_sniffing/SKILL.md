---
name: environment sniffing
description: 对当前所处环境的感知，暂时只支持目录文件的信息获取
---

## Quick start
### get directory files info
Run this script from this file's directory: `python scripts/directoryFiles.py`. It will get current directory files info.

### get os info
run this command `cat /etc/os-release`. it will show os info